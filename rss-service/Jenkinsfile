pipeline {
    agent any
    tools { 
        maven 'Maven 3.6.1'
        jdk 'jdk8' 
    }
    stages {
        stage ('Initialize') {
           steps {
               bat '''
                   echo "PATH = ${PATH}"
                   echo "MAVEN_HOME = ${MAVEN_HOME}"
              '''
       		}
		}
        stage('Build') {
            steps {
                echo 'Building..'
                bat '''
                	cd rss-service
                	mvn clean install -DskipTests
                '''
 	       }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                bat '''
                	cd rss-service
                	mvn test
                '''
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}