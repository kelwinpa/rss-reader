node {
    withMaven(maven:'maven') {

        stage('Checkout') {
            git url: 'https://github.com/kelwinpa/rss-reader.git'
        }

        stage('Build') {
  				sh "./mvnw clean install -DskipTests"
        } 
    }
}